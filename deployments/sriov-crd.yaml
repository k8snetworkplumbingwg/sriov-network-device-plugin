apiVersion: "kubernetes.cni.cncf.io/v1"
kind: Network
metadata:
  name: sriov-net1
spec:
  config: '{
	"type": "sriov",
        "name": "sriov-network",
	"ipam": {
		"type": "host-local",
		"subnet": "10.56.217.0/24",
		"routes": [{
			"dst": "0.0.0.0/0"
		}],
		"gateway": "10.56.217.1"
	}
}'
